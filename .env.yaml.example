# Environment configuration for AI assistant deployment
# Copy this file to .env.yaml and replace with your actual values

# LLM API Keys - Configure the appropriate key based on your llm.engines setting in assistant-config.yaml

# OpenAI API Key (required when "openai" is in llm.engines)
openai_api_key: "your-openai-api-key-here"

# vLLM API Token (optional - only needed if your vLLM server requires authentication)
# For local vLLM without authentication, you can omit this or leave it empty
# vllm_api_token: "your-vllm-api-token-here"

# Ollama API Key (optional - only needed if your Ollama server requires authentication)
# For local Ollama without authentication, you can omit this or leave it empty
# ollama_api_key: "your-ollama-api-key-here"

# Additional environment variables can be added here as needed
# These will be automatically loaded into the Kubernetes secret during deployment
tavily_api_key: "your-tavily-web-search-api-key-here"